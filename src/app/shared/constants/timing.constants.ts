/**
 * Timing constants used throughout the application
 */

/** Duration of the vinyl spin animation in milliseconds */
export const SPIN_ANIMATION_DURATION_MS = 2000;

/** Delay between Discogs API requests to avoid rate limiting */
export const DISCOGS_API_DELAY_MS = 1000;

/** Duration to show sync status message before clearing */
export const SYNC_MESSAGE_DISPLAY_MS = 3000;

/** Delay before transitioning from sync screen */
export const SYNC_TRANSITION_DELAY_MS = 1500;

/** Default days since last played when no date is available */
export const DEFAULT_DAYS_SINCE_PLAYED = 365;
